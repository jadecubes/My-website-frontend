import { ContactClient } from '@/components/pages/ContactClient';

export const metadata = {
  title: 'Contact — Ethos Studio',
};

export default function ContactPage() {
  return <ContactClient />;
}
